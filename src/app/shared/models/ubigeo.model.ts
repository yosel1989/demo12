export interface DepartmentDto{
    code: string;
    name: string;
}

export interface ProvinceDto{
    code: string;
    name: string;
}

export interface DistrictDto{
    code: string;
    name: string;
}